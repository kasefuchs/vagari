#!/usr/bin/env bash

export VAGRANT_CWD="@VAGARI_PATH@"

export VAGARI_WORKDIR="$PWD"
export VAGRANT_DOTFILE_PATH="${VAGRANT_DOTFILE_PATH:-$VAGARI_WORKDIR/.vagrant}"

if [[ -f "$1" ]]; then
  export VAGARI_CONFIG="$(realpath "$1")"
  shift
fi

exec vagrant "$@"
